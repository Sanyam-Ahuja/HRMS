// Backup file: minimal stub to avoid build errors. The real chatbot page
// implementation lives in `src/app/chatbot/page.tsx`.

'use client';

export default function ChatbotPageBackup() {
  return null;
}
